One file per phase, named for the phase: `phase-000-bootstrap.md`, then `volume-01-batch-0001.md`.

Each file records what the reviewer checked, what was found, what was changed, and what was deliberately left alone. A review that finds nothing is still written; the empty findings section is the evidence.
